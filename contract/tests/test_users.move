#[test_only]
module paylancer_addr::test_users {
    #[test(sender = @paylancer_addr)]
    public fun test_users_placeholder(_sender: &signer) {
        // Placeholder test for users.move
        // Add real tests when contract logic is implemented
        assert!(true, 0);
    }
}
