#[test_only]
module paylancer_addr::test_virtual_cards {
    #[test(sender = @paylancer_addr)]
    public fun test_virtual_cards_placeholder(_sender: &signer) {
        // Placeholder test for virtual_cards.move
        assert!(true, 0);
    }
}
